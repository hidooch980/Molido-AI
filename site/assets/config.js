/*
 * پیکربندیِ سایتِ ایستا.
 *
 * ⚠️ تنها فایلی که هنگام آپلود روی cPanel باید عوض شود.
 *
 *    جدا نگه داشتنش یعنی به‌روزرسانیِ بعدیِ سایت، با جایگزینیِ ساده
 *    انجام می‌شود بدونِ اینکه کسی یادش برود نشانی‌ها را دوباره بنویسد.
 *
 * ⚠️ `apiBase` باید نشانیِ **بک‌اند** باشد، نه خودِ این سایت.
 *
 *    سایت روی هاستِ اشتراکی است و هیچ کدِ سروری ندارد؛ سفارش و
 *    پرداخت به سرورِ اصلی می‌روند چون تأییدِ پرداخت باید سمتِ سرور
 *    انجام شود.
 *
 * ⚠️ حتماً https باشد.
 *
 *    با http، مرورگر درخواستِ صفحهٔ امن به نشانیِ ناامن را مسدود
 *    می‌کند (mixed content) و فرم بی‌صدا کار نمی‌کند — خطایی که فقط
 *    در کنسول دیده می‌شود.
 */
window.MOLIDO_CONFIG = {
  apiBase: 'https://194.5.176.140/api',
  panelUrl: 'https://194.5.176.140/panel',
};
