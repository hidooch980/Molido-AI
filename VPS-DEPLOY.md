# استقرار روی سرور اینترنتی

راهنمای نصب Molido روی یک VPS تازه (اوبونتو ۲۲/۲۴).

> **تفاوت مهم با نصب محلی.** روی شبکهٔ فروشگاه، بدترین اتفاق این است که
> یک کارمند به جایی سرک بکشد. روی اینترنت، از ساعت اول اسکن‌کننده‌های
> خودکار سراغ سرور می‌آیند و `admin123` را امتحان می‌کنند. به همین دلیل
> `deploy-vps.sh` رمز پیش‌فرض را قبول نمی‌کند و هیچ پورتی جز ۸۰ و ۴۴۳ باز
> نمی‌گذارد.

---

## ۰) پیش از شروع — دامنه

اگر می‌شود، یک دامنه بگیرید و به آی‌پی سرور وصلش کنید (رکورد `A`).

با **آی‌پی خالی** گواهی معتبر ممکن نیست و مرورگر هر بار هشدار می‌دهد.
کاربری که یاد بگیرد هشدار گواهی را رد کند، دیگر جعل سایت را هم تشخیص
نمی‌دهد — همان عادت، خطر اصلی است.

با دامنه، Caddy معمولاً خودش گواهی رایگان می‌گیرد و تمدید می‌کند.

**ولی یک شرط دارد که سند قبلاً نمی‌گفت:** سرور باید از **بیرون** در
دسترس باشد.

Let's Encrypt برای اثبات مالکیت، از سرورهای خودش به شما وصل می‌شود —
`http-01` روی پورت ۸۰ و `tls-alpn-01` روی ۴۴۳.  اگر شبکهٔ بالادست
اتصال ورودی از خارج را ببندد، هر دو با «Timeout during connect»
شکست می‌خورند، حتی وقتی فایروال خودِ سرور کاملاً باز است.

روی ۱۹۴.۵.۱۷۶.۱۴۰ دقیقاً همین شد.  آزموده شده:

| سنجش | نتیجه |
|---|---|
| `ufw status` (۲۲/۸۰/۴۴۳) | باز |
| سرور → Let's Encrypt | ۲۰۰ |
| دسترسی از داخل ایران | ۲۰۰ |
| دسترسی از پراکسی بیرون ایران | **۵۲۲ تایم‌اوت** |

### اگر ورودی از بیرون بسته است

تنها راه، چالش **`DNS-01`** است: سرور یک رکورد TXT در DNS می‌سازد و
Let's Encrypt همان را می‌خواند.  هیچ اتصال ورودی لازم نیست.

لازمه‌اش سه چیز است، نه فقط دامنه:

1. دامنه
2. ارائه‌دهندهٔ DNS با API — Cloudflare رایگان کافی است
3. تصویر Caddy با افزونهٔ DNS آن ارائه‌دهنده (`caddy-dns/cloudflare`)؛
   تصویر رسمی این افزونه را ندارد و باید با `xcaddy` ساخته شود

سپس در `Caddyfile`:

```
tls {
	dns cloudflare {env.CF_API_TOKEN}
}
```

`Caddyfile.with-domain` این ساختار را آماده دارد.

### چطور بفهمید کدام حالت را دارید

پیش از خرید دامنه، این را بزنید:

```bash
curl -s -o /dev/null -w '%{http_code}\n' \
  "https://api.codetabs.com/v1/proxy?quest=http://YOUR-SERVER-IP/"
```

اگر ۲۰۰ داد، ورودی باز است و دامنهٔ ساده کافی است.
اگر ۵۲۲ یا تایم‌اوت داد، باید سراغ `DNS-01` بروید.

---

## ۱) آماده‌سازی سرور

با SSH وارد شوید:

```bash
ssh root@194.5.176.140
```

### داکر

```bash
curl -fsSL https://get.docker.com | sh
```

### حافظه

بیلد Next.js به حدود ۲ گیگابایت نیاز دارد. اگر سرور کمتر دارد، پیش از
نصب swap بسازید — وگرنه بیلد با `exit code 137` کشته می‌شود و پیام خطا
هیچ اشاره‌ای به حافظه ندارد:

```bash
fallocate -l 2G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile && echo '/swapfile none swap sw 0 0' >> /etc/fstab
```

### دیوار آتش

```bash
apt install -y ufw && ufw allow 22/tcp && ufw allow 80/tcp && ufw allow 443/tcp && ufw enable
```

فقط این سه پورت. دیتابیس، n8n و بک‌اند اصلاً نباید از بیرون دیده شوند.

---

## ۲) آوردن کد

```bash
mkdir -p /opt && cd /opt && git clone <آدرس-مخزن> molido && cd molido
```

یا اگر مخزن گیت ندارید، از همان دستگاهی که الان کد رویش است:

```bash
rsync -av --exclude node_modules --exclude .next --exclude .git ./ root@194.5.176.140:/opt/molido/
```

---

## ۳) نصب

```bash
cd /opt/molido && bash deploy-vps.sh
```

اسکریپت این‌ها را می‌پرسد:

| پرسش | توضیح |
|---|---|
| دامنه یا آی‌پی | همان که در گام ۰ تنظیم کردید |
| ایمیل | فقط با دامنه — برای هشدار انقضای گواهی |
| رمز مدیر | دست‌کم ۱۲ نویسه؛ `admin123` رد می‌شود |

بقیهٔ رمزها (دیتابیس، JWT، n8n) خودکار و تصادفی ساخته می‌شوند و در
`.env` می‌نشینند — که فقط برای `root` خواندنی است.

بار اول ۵ تا ۱۵ دقیقه طول می‌کشد.

### اجرای دوباره

بی‌ضرر است. مقادیر موجود در `.env` دست نمی‌خورند، پس رمز دیتابیس عوض
نمی‌شود (که کل سامانه را از کار می‌انداخت).

---

## ۴) بررسی پس از نصب

اسکریپت خودش این‌ها را می‌سنجد و نتیجه را چاپ می‌کند:

- API پاسخ می‌دهد
- پنل باز می‌شود
- **رمز پیش‌فرض کار نمی‌کند**
- هیچ پورت داخلی روی اینترنت باز نیست
- دیوار آتش فعال است

برای بررسی کامل‌تر (۶۰ مورد):

```bash
MOLIDO_COMPOSE="docker compose -f docker-compose.yml -f docker-compose.store.yml -f docker-compose.vps.yml" MOLIDO_API=https://YOUR-HOST/api MOLIDO_WEB=https://YOUR-HOST bash deploy-check.sh
```

---

## ۵) سخت‌سازی SSH

سرور فعلی ورود با **رمز** را هم قبول می‌کند. پس از اینکه مطمئن شدید
ورود با کلید کار می‌کند، رمز را ببندید:

```bash
sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config && systemctl reload sshd
```

> پیش از این کار، در یک ترمینال دیگر ورود با کلید را امتحان کنید. اگر
> کلید کار نکند و رمز هم بسته باشد، راه بازگشتی جز کنسول ارائه‌دهنده
> نمی‌ماند.

---

## ۶) کارهای روزمره

### پشتیبان

روزانه، هفتگی و ماهانه خودکار گرفته می‌شود.

```bash
C="docker compose -f docker-compose.yml -f docker-compose.store.yml -f docker-compose.vps.yml"

$C exec backup ls -la /backups/daily      # فهرست
$C exec backup restore.sh --list          # با جزئیات
$C exec backup restore.sh --verify daily/molido-YYYYMMDD-HHMMSS.sql.gz
```

`--verify` پشتیبان را در یک دیتابیس موقت بازمی‌گرداند و بعد پاکش می‌کند.
**ماهی یک بار انجامش بدهید.** پشتیبانی که بازیابی‌اش آزموده نشده، پشتیبان
نیست.

پشتیبان‌ها روی همان سرورند. برای امنیت واقعی، جای دیگری هم کپی کنید:

```bash
rsync -av root@194.5.176.140:/var/lib/docker/volumes/molido_backup_data/_data/ ~/molido-backups/
```

### n8n

عمداً از اینترنت باز نیست: هر کسی که واردش شود می‌تواند گردش‌کاری بسازد
که به API فروشگاه دستور بدهد. با تونل SSH بازش کنید:

```bash
ssh -L 5678:localhost:5678 root@194.5.176.140
```

سپس در مرورگر خودتان `http://localhost:5678`. کاربر و رمزش در `.env`.

### به‌روزرسانی

```bash
cd /opt/molido && git pull && bash deploy-vps.sh
```

مهاجرت‌های دیتابیس خودکار اجرا می‌شوند و تکرارشان بی‌اثر است.

### لاگ

```bash
$C logs -f backend      # خطاهای برنامه
$C logs -f caddy        # مشکل گواهی و مسیریابی
```

صفحهٔ **عملیات و سلامت** در خود پنل هم خطاها را گروه‌بندی شده نشان
می‌دهد — معمولاً از لاگ خام مفیدتر است.

---

## اشکالات رایج

| نشانه | علت | چاره |
|---|---|---|
| بیلد با `exit code 137` می‌میرد | حافظه کم | swap بسازید (گام ۱) |
| مرورگر هشدار گواهی می‌دهد | با آی‌پی نصب شده | دامنه بگیرید |
| گواهی گرفته نمی‌شود | دامنه به سرور اشاره نمی‌کند، یا ۸۰ بسته است | `dig +short YOUR-DOMAIN` و `ufw status` |
| گواهی با «Timeout during connect» شکست می‌خورد | ورودی از **بیرون** بسته است — فایروال سرور بی‌تقصیر است | گام ۰ را بخوانید؛ نیاز به `DNS-01` دارید |
| `502 Bad Gateway` | بک‌اند بالا نیامده | `$C logs backend` |
| ورود نمی‌شود | رمز عوض شده | `$C exec backend node dist/database/seed.js` با `ADMIN_PASSWORD` تازه در `.env` |
| `429` بی‌دلیل | `TRUST_PROXY` تنظیم نشده | در `docker-compose.vps.yml` هست؛ بررسی کنید اعمال شده |

---

## دو محصول روی یک سرور

فروشگاه و کافه‌رستوران دو **پروژهٔ داکرِ جدا**ند: پایگاه‌دادهٔ جدا،
کانتینرِ جدا، دادهٔ جدا.  فقط لبه (Caddy) مشترک است.

```bash
docker network create molido_edge          # یک بار

# فروشگاه — همان‌جا که بود می‌ماند (آی‌پی یا دامنهٔ اصلی)
docker compose -f docker-compose.yml -f docker-compose.store.yml   -f docker-compose.vps.yml -f docker-compose.edge-store-ip.yml   -f docker-compose.edge-caddy.yml up -d

# رستوران — روی پورتِ دومِ TLS
docker compose -p molido-resto -f docker-compose.yml -f docker-compose.resto.yml   -f docker-compose.vps.yml -f docker-compose.edge-resto.yml   up -d postgres migrate backend web
```

در `.env`:

```
MOLIDO_HOST_RESTO=<آی‌پی>:8443
MOLIDO_RESTO_PORT=8443
MOLIDO_EDGE_DEFAULT=store
MOLIDO_TLS_SUB=tls internal
```

⚠️ **چرا پورت و نه زیردامنه.**  زیردامنه راهِ درست است، ولی این سرور از
بیرونِ ایران در دسترس نیست و Let's Encrypt هرگز گواهی نمی‌دهد.  زیردامنهٔ
بی‌گواهی یعنی مرورگر اصلاً صفحه‌ای نشان نمی‌دهد — بدتر از پورت.  با دامنه
و چالشِ DNS-01، همان متغیر می‌شود `cafe.example.ir` و پورت بی‌استفاده
می‌ماند.

⚠️ **`MOLIDO_HOST_STORE` را روی آی‌پی نگذارید.**  بلوکِ پایهٔ
`Caddyfile.subdomains` از قبل آی‌پی را می‌گیرد؛ دو بلوک با یک نشانی یعنی
`ambiguous site definition` و **کلِ** پیکربندی رد می‌شود — هر دو محصول
می‌خوابند.

⚠️ **`HOSTNAME=0.0.0.0` برای سرویسِ وب لازم است.**  خروجیِ standalone نکست
روی `process.env.HOSTNAME` می‌شنود و داکر آن را شناسهٔ کانتینر می‌گذارد،
که فقط به شبکهٔ **اول** حل می‌شود.  از شبکهٔ لبه «connection refused»
می‌گیرید در حالی که لاگ «Ready» نوشته و کانتینر سالم است.

⚠️ **حافظه.**  استکِ دوم حدود ۷۰۰ مگابایت می‌خواهد.  روی سرورِ ۶ گیگی،
`n8n` و `backup` را برای محصولِ دوم بالا نیاورید.


## اشتراک و فروش

- `/vendor` — پنلِ فروشنده (فقط `SUPER_ADMIN`): فهرست مشتریان، انقضا،
  تمدید و تعلیق.  حساب با `bash ops/create-vendor.sh` ساخته می‌شود.
- `/billing` — صفحهٔ مشتری: نسخهٔ فعلی، خرید تمدید از درگاه، تاریخچهٔ
  صورت‌حساب.
- قیمت‌ها در جدولِ `PlanDefault."priceRial"`اند، نه در کد.  نسخه‌ای که
  قیمت ندارد آنلاین فروخته نمی‌شود («تماس بگیرید»).

⚠️ اشتراکِ منقضی **نوشتن** را می‌بندد، نه خواندن.  دادهٔ مشتری گروگان
نیست؛ ولی فروش متوقف می‌شود.  `/auth`، `/subscription` و `/billing` باز
می‌مانند تا تمدید ممکن باشد.


## آنچه این نصب **ندارد**

صادقانه، تا کسی رویش حساب باز نکند:

- **کلیدِ درگاه پرداخت.**  کدِ کامل هست و آزموده شده، ولی
  `ZARINPAL_MERCHANT_ID` را باید شما بگذارید: `bash ops/set-payment.sh`.
- **فروشِ کالای امانیِ گرفته‌شده از صندوق.**  ثبت و تسویه‌اش هست، ولی
  از صندوق فروخته نمی‌شود: مسیرِ درستش باید سندِ `SaleCogs` را از آن سطر
  حذف کند (وگرنه بهای تمام‌شده دو بار می‌خورد) و شاخهٔ مرجوعیِ جداگانه
  دارد.
- **ارسال واقعی به سامانهٔ مؤدیان.** ساختار صورتحساب و شناسهٔ یکتا آماده
  است، ولی نگاشت میدان‌ها و امضای دیجیتال با دادهٔ واقعی آزموده نشده.
  شناسهٔ حافظهٔ مالیاتی و کلید خصوصی شما لازم است.
- **مقیاس چندسروری.** یک سرور، یک دیتابیس. برای فروشگاه و حتی چند شعبه
  کافی است؛ برای ده‌ها هزار تراکنش در دقیقه نه.
