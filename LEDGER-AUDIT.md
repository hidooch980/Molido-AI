# ممیزیِ دفترکل — «پول جابه‌جا شد، دفتر خبر دارد؟»

این فایل هست تا **این بررسی دوباره از صفر تکرار نشود**.

## خانوادهٔ اشکال

پولی جابه‌جا می‌شود (`balance = balance ± x`) ولی هیچ سندی در دفترکل
زده نمی‌شود.

⚠️ **هیچ آزمونی این را نمی‌گیرد، و دلیلش ظریف است:**

> تراز آزمایشی **صفر می‌ماند**. وقتی اصلاً سندی زده نمی‌شود، چیزی هم
> نامتراز نمی‌شود.

پس «تراز صفر است» با «دفتر درست است» یکی نیست. سامانه سالم به نظر
می‌رسد چون نیمی از یک جفت اصلاً وجود ندارد.

## آنچه پیدا و بسته شد

| مسیر | چه بود | مدرک |
|---|---|---|
| خریدِ دارایی | سند نمی‌خورد | ۱۲۰۱ ماندهٔ بستانکار داشت |
| `PATCH /cashbox/:id/deposit` | نه سند، **نه ردِ حسابرسی** | واریز ۱٬۰۰۰٬۰۰۰ → ۱۱۰۱ صفر تکان خورد |
| `PATCH /cashbox/:id/withdraw` | همان | صندوق‌ها ۵۶٬۴۴۰٬۰۰۰ در برابر ۱۱۰۱ برابر ۳۶٬۳۴۰٬۰۰۰ |
| `POST /treasury/transactions` | سند نمی‌خورد (ردِ حسابرسی داشت) | — |
| `POST /payments` | **مطالبات هرگز تسویه نمی‌شد** | پرداخت ۱۰۰٬۰۰۰ → ۱۱۰۱ و ۱۱۰۳ هر دو صفر |
| `POST /restaurant/orders/:id/settle` | کلِ درآمد بیرونِ دفتر | ۳۸ سفارشِ پرداخت‌شده، جمع ۱۹٬۲۴۰٬۰۰۰، **صفر سند** |

`POST /payments` بدترینِ منطقی بود: فروش سند می‌زد (دریافتنی بدهکار)
ولی وصولش آن بدهی را پاک نمی‌کرد. مشتری در دفتر برای همیشه بدهکار
می‌ماند در حالی که پول در صندوق بود.

## آنچه بررسی شد و **اشکال نبود**

- **`ration`** — `RationAccount.balance` سهمیهٔ یارانه‌ایِ مشتری است،
  نه پولِ شرکت. داخلِ تراکنشِ خودِ فروش خرج می‌شود (`spendIn` با
  `saleId`) و در `paid` فاکتور می‌نشیند، پس سندش با سندِ فروش می‌خورد.
- **انتقالِ بین دو حسابِ خزانه** — اثرِ خالصش روی دفتر صفر است وقتی
  هر دو به یک حسابِ کل می‌نشینند. سند زدنش فقط دفتر را شلوغ می‌کند.

## نگهبانِ خودکار

`backend/test/ops-scripts.sh` بخش ۴ — هر فایلی که `balance = balance`
دارد باید `postAuto(` یا `postIn(` هم داشته باشد.

⚠️ **دو بار خودِ این نگهبان غلط بود**، و هر دو با آزمونِ منفی گرفته شد:

1. الگوی `posting\.` خطِ `import '../accounting/posting.service'` را
   هم «سند زدن» می‌شمرد — پس با تزریقِ عمدیِ خطا سبز می‌ماند.
2. حالا `postAuto\(|postIn\(` است: **فراخوانی** را می‌سنجد نه نامِ
   ماژول را.

استثناها در `LEDGER_EXEMPT` نام‌دارند و هر کدام دلیلش نوشته شده.

## روشِ سنجش (اگر باز هم لازم شد)

```bash
# ۱) کدام فایل‌ها موجودی را عوض می‌کنند؟
grep -rl 'balance = balance' backend/src --include=*.ts

# ۲) کدامشان سند نمی‌زنند؟
for f in $(grep -rl 'balance = balance' backend/src --include=*.ts); do
  grep -qE 'postAuto\(|postIn\(' "$f" || echo "بی‌سند: $f"
done
```

⚠️ نبودِ `grep` **اثبات نیست**. هر مورد باید با اندازه‌گیریِ واقعی
تأیید شود: ماندهٔ حساب را پیش و پس از عملیات بخوانید.

```bash
gl() { psql -tAc "SELECT COALESCE(round(sum(l.debit-l.credit)),0)
  FROM \"JournalLine\" l
  JOIN \"Account\" a ON a.id=l.\"accountId\"
  JOIN \"JournalEntry\" e ON e.id=l.\"entryId\"
  WHERE a.code='$1' AND e.status<>'REVERSED';"; }
```

کدهای پرکاربرد: `1101` صندوق، `1102` بانک، `1103` دریافتنی،
`3101` سرمایه، `4101` درآمد فروش، `5101` بهای تمام‌شده.

## «بابت» — چرا اجباری است

طرفِ دومِ سند از منشأ پول می‌آید و سامانه نمی‌تواند حدسش بزند:

| بابت | طرفِ دوم |
|---|---|
| `OWNER` | سرمایه (۳۱۰۱) |
| `BANK` | بانک (۱۱۰۲) |
| `ADJUST` / `OTHER` | درآمد/هزینهٔ متفرقه |

⚠️ حدس زدنش یعنی دفتری که عددهایش تراز است و معنایش غلط — و آن بدتر
از نامتراز بودن است، چون کسی شک نمی‌کند.

## کارِ باقی‌مانده

- ⚠️ از ۴ قلمِ منوی رستوران فقط **۱** بهای تمام‌شده دارد. تا پر نشود،
  سودِ ناخالصِ رستوران واقعی نیست — کد درست است، داده کم است.
- سطرهای گذشتهٔ `RestaurantOrderItem` عمداً `unitCost` تهی دارند.
  پر کردنشان با بهای امروز، عددِ ساختگیِ **نامرئی** می‌ساخت.
- سفارش‌های رستورانِ پیش از این اصلاح سند ندارند. اگر لازم است،
  مهاجرتِ عطف‌به‌ماسبق می‌خواهد — مثل ۰۶۴ که برای داراییِ واگذارشده
  نوشته شد.
