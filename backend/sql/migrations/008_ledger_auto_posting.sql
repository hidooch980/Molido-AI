-- فعال‌سازی ثبت خودکار در دفتر کل
--
-- ثبت خودکار عمداً پیش‌فرضِ خاموش است.  شرکتی که هنوز کدینگ حساب و سال مالی
-- تعریف نکرده، اگر ناگهان هر فروشش نیاز به سند داشته باشد اصلاً نمی‌تواند
-- بفروشد.  با این پرچم، حسابداری وقتی روشن می‌شود که آماده باشد — و از آن
-- لحظه به بعد، سند نخوردن یک فاکتور یک خطاست، نه سکوت.

ALTER TABLE "Company" ADD COLUMN IF NOT EXISTS
  "ledgerEnabled" BOOLEAN NOT NULL DEFAULT false;
