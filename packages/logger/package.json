{
  "name": "@molido/logger",
  "version": "0.1.0",
  "private": true,
  "description": "Structured, redacting logger for MOLIDO AI services.",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@molido/security": "workspace:*",
    "@molido/types": "workspace:*",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  },
  "peerDependencies": {
    "pino-pretty": "^13.1.3"
  },
  "peerDependenciesMeta": {
    "pino-pretty": { "optional": true }
  }
}
