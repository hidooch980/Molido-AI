{
  "name": "@molido/queue",
  "version": "0.1.0",
  "private": true,
  "description": "BullMQ queue definitions shared by the MOLIDO API and its workers.",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@molido/types": "workspace:*",
    "bullmq": "^5.81.3",
    "ioredis": "^5.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
