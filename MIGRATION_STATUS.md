# وضعیت لایهٔ داده — Molido AI

> این فایل حافظهٔ کوتاه پروژه است. قبل از هر کار بخوانید، بعد از هر کار به‌روزرسانی کنید.
> فقط جدول سنجه و چک‌لیست را عوض کنید؛ توضیح اضافه ننویسید.

## وضعیت: مهاجرت Prisma → SQL خام **کامل شد** (۲۰۲۶-۰۸-۰۹)

Prisma به‌طور کامل حذف شده. تمام دسترسی به دیتابیس از `DatabaseService`
(`pg` خام، پارامتر positional) انجام می‌شود.

## فرمان‌های تست

```bash
cd backend && npx tsc --noEmit -p tsconfig.json          # ۰ خطا (شامل test/)
cd backend && npm run build                              # سبز
cd backend && npx jest                                   # ۱۷ تست
cd backend && DATABASE_URL=... npm run check:schema       # نگهبان drift
cd backend && DATABASE_URL=... npm run db:migrate
cd backend && DATABASE_URL=... npm run test:smoke         # ۶۰ بررسی روی PostgreSQL واقعی
cd backend && DATABASE_URL=... npm run test:ledger        # ۴۶ بررسی دفتر کل
cd backend && npm run build && npm run test:products      # ۲۰ بررسی جداسازی محصول
cd backend && npm run test:store -- http://SERVER:3000    # ۳۶ بررسی روی سامانهٔ در حال اجرا
cd web && npm run build                                  # ۹ مسیر
```

| تاریخ | خطای tsc | سرویس روی Prisma |
|---|---|---|
| 2026-08-09 (شروع) | 587 | 77 |
| 2026-08-09 (پایان) | 0 | 0 |

## معماری لایهٔ داده

- `src/database/database.service.ts` — استخر pg، `query` / `execute` / `transaction`
- `src/database/base-crud.service.ts` — CRUD عمومی شرکت‌محور؛ ۵۳ سرویس از آن ارث می‌برند
- `src/database/schema.generated.ts` — نقشهٔ جدول → ستون (whitelist ستون‌ها در CRUD)
- `src/database/sql.ts` — `Params` و `setClause` برای SQL دستی
- `src/inventory/inventory.service.ts` → `applyStockDelta()` — تنها نقطهٔ تغییر موجودی
  انبار؛ فروش، خرید و انتقال همه از آن استفاده می‌کنند
- `sql/migrations/001_identity.sql` + `002_core_schema.sql` — ۱۳۶ جدول، ۱۸۱ FK، ۱۶۲ index
- `003_revenue_receipts.sql` — جدول `Receipt`، نقطهٔ واحد دریافت وجه همهٔ زیرسیستم‌ها
- `004_retail_pos.sql` — کالای وزنی، بارکد ترازو، `CashierShift`، `Sale.shiftId`
- `005_ration_coupon.sql` — کالابرگ: `RationAccount`، `RationTransaction`
- `006_currency.sql` — واحد پول شرکت (ریال، تومان، درهم، دلار، یورو، لیر)
- `007_general_ledger.sql` — دفتر کل: `FiscalYear`، `JournalEntry`، `JournalLine`
- `008_ledger_auto_posting.sql` — `Company.ledgerEnabled`

## لایه‌های مشترک

- `src/revenue/revenue.service.ts` — `collect()` تراکنشی: رسید + واریز به صندوق/خزانه
  + audit + رویداد n8n.  ایندکس یکتای `(entityType, entityId)` پرداخت دوباره را
  در سطح دیتابیس می‌بندد
- `src/audit-log/audit-trail.service.ts` — `record()` بی‌خطر و `recordIn(tx)` تراکنشی
- `src/ration/ration.service.ts` — کالابرگ؛ `spendIn(tx)` و `reverseIn(tx)` داخل
  تراکنش فروش صدا زده می‌شوند
- `src/retail/barcode.ts` — تجزیهٔ بارکد ترازو، خالص و بدون وابستگی (۱۰ تست)
- `src/ai/llm.service.ts` — آداپتور مدل زبانی سازگار با OpenAI؛ `AI_API_KEY` خالی
  یعنی غیرفعال و بازگشت به تحلیل داخلی

## قواعد

طبق `PROJECT_CHAPTERS.md`:

1. مقادیر همیشه پارامتر positional (`$1`)؛ نام ستون فقط از whitelist ثابت کد.
2. تغییر schema فقط با یک فایل **جدید** در `backend/sql/migrations/` — فایل‌های
   موجود ویرایش نمی‌شوند (`migrate.ts` آن‌ها را یک‌بار اجرا و در `schema_migrations` ثبت می‌کند).
3. `schema.generated.ts` باید با migrationها هم‌گام بماند؛ ستون جدید را دستی اضافه کنید.
4. سرویس CRUD ساده → `BaseCrudService`. منطق خاص → SQL دستی با `Params`.
5. `legacy/` وارد اجرای اصلی نمی‌شود.

## لایهٔ درآمد مشترک (۲۰۲۶-۰۸-۱۰)

`Receipt` نقطهٔ واحد ثبت دریافت وجه برای **همهٔ** زیرسیستم‌هاست.

- `src/revenue/revenue.service.ts` → `collect()` تراکنشی: رسید + شارژ صندوق/خزانه + audit + رویداد n8n
- `src/audit-log/audit-trail.service.ts` → نویسندهٔ audit (جدا از CRUD خواندنی)
- `sql/migrations/003_revenue_receipts.sql` — قیدها در سطح دیتابیس:
  - `UNIQUE (entityType, entityId)` → پرداخت دوباره غیرممکن است حتی در شرایط رقابتی
  - `CHECK` → مقصد وجه دقیقاً یکی از صندوق یا حساب خزانه
  - `CHECK amount > 0`
- `MunicipalFeesService.pay()` حالا واقعاً پول را وارد صندوق می‌کند
- `AccountingService.summary()` فیلدهای `otherRevenue`، `otherRevenueByType`،
  `totalRevenue` را برمی‌گرداند

**برای وصل کردن زیرسیستم درآمدی جدید:** `RevenueService.collect()` را با
`entityType` خودش صدا بزنید. هیچ تغییری در جدول `Receipt` لازم نیست.

## هستهٔ خرده‌فروشی (۲۰۲۶-۰۸-۱۰)

`sql/migrations/004_retail_pos.sql` + `src/retail/`

- **بارکد ترازو** — `src/retail/barcode.ts`: ماژول خالص و بدون وابستگی.
  عرض فیلد مقدار از طول پیشوند مشتق می‌شود، پس پیشوند یک‌رقمی و دورقمی هر دو
  کار می‌کنند. رقم کنترل EAN-13 بررسی می‌شود تا اسکن خراب مبلغ فاکتور را
  خراب نکند. ۱۰ تست واحد.
- **کالای وزنی** — `Product.isWeighed` و `Product.scaleCode` (یکتا در هر شرکت)
- **تنظیم بارکد ترازو در سطح شرکت** — `Company.scaleBarcodePrefix` و
  `scaleBarcodeMode` (`WEIGHT` یا `PRICE`)
- **اسکن** — `ScanService.scan()` سه حالت را به ترتیب امتحان می‌کند:
  برچسب ترازو ← بارکد کالا ← SKU
- **شیفت صندوق‌دار** — `CashierShift` با مغایرت‌گیری نقدی. قیدهای دیتابیس:
  ایندکس یکتای جزئی روی `endedAt IS NULL` → یک شیفت باز برای هر صندوق‌دار،
  و یک شیفت باز برای هر صندوق
- **تسویهٔ چندبخشی** — `CreateSaleDto.payments[]` (نقد + کارت روی یک فاکتور)؛
  شکل قدیمی `paidAmount`/`paymentMethod` برای سازگاری باقی مانده
- **اتصال فاکتور به شیفت** — `Sale.shiftId` خودکار از شیفت باز کاربر پر می‌شود

## دفتر کل (۲۰۲۶-۰۸-۱۰)

`sql/migrations/007_general_ledger.sql` + `src/accounting/`

قواعد حسابداری **در سطح دیتابیس** اعمال می‌شوند، نه فقط در کد — حتی اگر کسی
مستقیم SQL بزند، سند نامتوازن ثبت نمی‌شود.  ۹ قید با psql آزموده شده:

- **توازن سند** — تریگر `DEFERRABLE INITIALLY DEFERRED`؛ در لحظهٔ commit
  بررسی می‌شود تا سرویس بتواند اقلام را یکی‌یکی درج کند
- **هر قلم یک طرف** — `CHECK ((debit = 0) <> (credit = 0))`
- **حداقل دو قلم** در هر سند
- **سال مالی بسته** سند نمی‌پذیرد (تریگر `BEFORE INSERT`)
- **بازهٔ سال‌های مالی هم‌پوشانی ندارد** — `EXCLUDE USING gist`
- **کد حساب per-company یکتاست** — پیش از این سراسری بود؛ دو شرکت نمی‌توانستند
  هر دو کد «۱۰۰۱» داشته باشند
- **هر سند مبنا یک سند فعال** — ایندکس یکتای جزئی روی
  `(companyId, sourceType, sourceId) WHERE status <> 'REVERSED'`

سرویس‌ها:

- `PostingService.postIn(tx, …)` — نقطهٔ واحد صدور سند.  عمداً `tx` می‌گیرد تا
  سند با همان تراکنش فاکتور commit یا rollback شود
- `PostingService.reverse()` — سند قطعی هرگز حذف نمی‌شود؛ سند معکوس صادر
  می‌گردد تا رد حسابرسی بماند
- `LedgerService` — تراز آزمایشی، دفتر معین با ماندهٔ تجمعی، صورت سود و زیان،
  ترازنامه

**نکتهٔ ظریف:** گزارش‌ها `status <> 'DRAFT'` فیلتر می‌کنند، نه `= 'POSTED'`.
سند خنثی‌شده باید در گزارش بماند چون سند معکوس دقیقاً همان را خنثی می‌کند؛
حذفش باعث می‌شد اثر برگشت **دو بار** اعمال شود (باگی که تست دفتر کل گرفت).

`seed.ts` کدینگ ۲۹ حسابی چهارگروهی و یک سال مالی باز می‌سازد.

## یکپارچه‌سازی دفتر کل (۲۰۲۶-۰۸-۱۰)

هر رویداد مالی خودکار سند می‌زند، **داخل همان تراکنش عملیات** — اگر سند نخورد،
فاکتور هم ثبت نمی‌شود و دفتر با عملیات هرگز از هم جدا نمی‌افتد.

| رویداد | سند |
|---|---|
| فروش | بدهکار صندوق/بانک/دریافتنی ← بستانکار فروش + مالیات |
| بهای تمام‌شده | بدهکار ۵۱۰۱ ← بستانکار موجودی کالا (سند جدا) |
| دریافت کالای خرید | بدهکار موجودی + مالیات خرید ← بستانکار پرداختنی |
| دریافت وجه (`Receipt`) | بدهکار صندوق/بانک ← بستانکار سایر درآمدها |
| هزینه | بدهکار حساب هزینه ← بستانکار صندوق یا پرداختنی |
| لغو فاکتور | سند معکوس خودکار |

- `src/accounting/posting-rules.ts` — همهٔ قاعده‌های «چه بدهکار، چه بستانکار»
  یک‌جا و به‌صورت توابع خالص؛ در سرویس‌های عملیاتی پخش نشده‌اند
- `PostingService.postAuto(tx, …)` — اگر `Company.ledgerEnabled` خاموش باشد
  هیچ کاری نمی‌کند.  **شرکتی که هنوز کدینگ حساب ندارد باید بتواند بفروشد**؛
  ولی وقتی روشن شد، شکست سند تراکنش را برمی‌گرداند
- سند در لحظهٔ **دریافت** کالا صادر می‌شود نه ثبت سفارش — تا آن لحظه نه کالایی
  رسیده و نه بدهی قطعی شده است
- `seed.ts` دفتر را برای شرکت نمونه روشن می‌کند

**بهای تمام‌شده فعلاً از `purchasePrice` لحظه‌ای کالا می‌آید.**  با پیاده‌سازی
میانگین موزون باید به لایه‌های واقعی موجودی تکیه کند.

## جداسازی محصول‌ها (۲۰۲۶-۰۸-۱۱)

سه محصول مستقل روی یک کد پایه:

| محصول | `MOLIDO_PRODUCT` | مسیر HTTP |
|---|---|---|
| فروشگاه و سوپرمارکت | `store` | ۱۸۸ |
| کافه‌رستوران | `resto` | ۲۰۳ |
| نسخهٔ کامل (شهرداری و سازمانی) | `suite` | ۳۳۶ |

هر محصول **دیتابیس، استقرار و نصب جداگانه** دارد؛ ولی **کد مشترک است**.  اگر
۸۰ ماژول کپی می‌شد، هر رفع باگ در حسابداری یا انبار باید چند بار انجام می‌شد و
نسخه‌ها از هم دور می‌افتادند.

- `src/product.ts` — تعریف محصول و قابلیت‌ها؛ مقدار نامعتبر عمداً خطا می‌دهد و
  بی‌صدا به `suite` برنمی‌گردد
- `src/app.module.ts` — `FEATURE_MODULES` نگاشت قابلیت به ماژول؛ ماژول‌ها در
  زمان اجرا بر اساس محصول بار می‌شوند
- `docker-compose.store.yml` / `docker-compose.resto.yml` — با `name:` جدا، پس
  هر دو محصول روی یک دستگاه کنار هم اجرا می‌شوند
- `setup.sh` / `setup.ps1` محصول را می‌پرسند
- `npm run test:products` — هر سه محصول را **واقعاً بوت می‌کند** و مرزها را
  می‌سنجد (روی `dist`، چون tsx متادیتای دکوراتور تولید نمی‌کند)

**چهار باگ واقعی که این تست پیدا کرد:**

1. `multer` در `package.json` نبود — فقط `@types/multer`.  آپلود فایل در
   production کار نمی‌کرد.
2. `ConfigModule` در چهار ماژول دوباره import شده بود؛ با `isGlobal: true` این
   یک نمونهٔ بدون تنظیمات می‌سازد.
3. `SalesService` وابستگی سخت به `CashierShiftService` داشت که در
   `RetailModule` بود — رستوران بدون صندوق فروشگاهی اصلاً بوت نمی‌شد.  شیفت به
   `CashierShiftModule` منتقل شد، چون مفهومش به فروش تعلق دارد نه اسکن بارکد.
4. `SalesService` و `AssistantService` وابستگی سخت به `RationService` داشتند؛
   با `@Optional()` حل شد.

## صف کارها (به ترتیب اولویت توافق‌شده)

### ✅ انجام‌شده
- مهاجرت کامل Prisma → SQL خام (۰ خطا، ۷۷ سرویس)
- لایهٔ درآمد مشترک (`Receipt`) + ثبت رویداد (`AuditTrail`)
- صندوق فروشگاهی: بارکد ترازو، کالای وزنی، شیفت با مغایرت‌گیری، تسویهٔ چندبخشی
- کالابرگ الکترونیکی با قیمت مصوب و کنترل اعتبار در سطح دیتابیس
- هستهٔ AI: آداپتور مدل زبانی + ۴ تحلیل فروشگاهی محلی
- استقرار تحت شبکه: `setup.sh` / `setup.ps1` با تشخیص خودکار IP
- CI با PostgreSQL واقعی + نگهبان drift
- چاپ رسید حرارتی ۸۰ میلی‌متری

### ۱ — تکمیل فروشگاه
- [ ] لایهٔ درگاه کارتخوان با آداپتور استاندارد (هر PSP یک آداپتور)
- [ ] انبارگردانی و شمارش موجودی
- [ ] صفحهٔ مدیریت کالابرگ در وب (الان فقط API دارد)

### ۲ — دفتر کل  ✅ **کامل شد**

بزرگ‌ترین شکاف ERP.  الان فروش، خرید، پرداخت و حقوق **هیچ سندی صادر نمی‌کنند**؛
`Account` فقط یک فهرست است.  نتیجه: ترازنامه و صورت سود و زیان واقعی قابل
استخراج نیست، فقط جمع‌های ساده.

- [x] `JournalEntry` + `JournalLine` با قید توازن در سطح دیتابیس
- [x] `PostingService` — نقطهٔ واحد صدور سند
- [x] تراز آزمایشی، دفتر معین، صورت سود و زیان، ترازنامه
- [x] سند معکوس، سال مالی و بستن دوره
- [x] کدینگ حساب پیش‌فرض در `seed.ts`
- [x] اتصال خودکار: فروش، بهای تمام‌شده، خرید، دریافت وجه، هزینه
- [ ] حقوق و دستمزد هنوز سند نمی‌زند
- [ ] بهای تمام‌شدهٔ واقعی (میانگین موزون) به‌جای `purchasePrice` لحظه‌ای

### ۳ — CRM واقعی  ⬅️ **بعدی**

`CrmService` فقط یک CRUD خالی روی `LoyaltyAccount` است.  قطعات پراکنده هست
(مشتری، تیکت، پیش‌فاکتور، ویزیتور، کوپن) ولی قلب CRM نیست:
سرنخ ← فرصت ← تعامل ← فعالیت، و تحلیل نرخ تبدیل و ارزش طول عمر مشتری.

### ۴ — رستوران  (محصول جدا شد ✅)
- [ ] اتصال رستوران به شیفت صندوق و `Receipt`
- [ ] تکمیل صفحهٔ وب رستوران

### ۵ — بودجه و اعتبارات شهرداری (بودجه‌ریزی عملیاتی)

**وضعیت فعلی ناکافی است.** `Budget` و `BudgetLine` فقط یک CRUD ساده‌اند
(`year`, `title`, `totalAmount`, `spentAmount`) و چیزی را کنترل نمی‌کنند.

بودجه‌ریزی عملیاتی شهرداری نیاز دارد به:

- **ساختار**: سال مالی، ردیف بودجه با کد طبقه‌بندی وزارت کشور،
  تفکیک درآمد / اعتبارات جاری / اعتبارات عمرانی
- **چرخهٔ اعتبار**: مصوب ← اصلاحیه/متمم ← تخصیص
- **چرخهٔ مصرف**: تعهد (encumbrance) ← هزینهٔ قطعی
  — تفاوت این دو همان چیزی است که الان اصلاً وجود ندارد
- **بُعد عملیاتی**: برنامه / فعالیت / پروژه با شاخص عملکرد و بهای تمام‌شدهٔ واحد
- **کنترل سخت**: تعهد بیش از اعتبار تخصیص‌یافته باید در سطح دیتابیس رد شود،
  مثل قید موجودی صندوق در `Receipt` و اعتبار در `RationAccount`
- **اتصال**: `Expense`، `Contract`، `Purchase` و `MunicipalBill` باید ردیف
  بودجه مصرف کنند؛ سمت درآمد از `Receipt` تغذیه می‌شود

### ۶ — امنیت و اداری
- [ ] RLS برای جداسازی شرکت‌ها (بزرگ‌ترین ریسک باقی‌مانده)
- [ ] اعتبارسنجی ورودی برای ۵۳ سرویس منتقل‌شده با codemod
- [ ] صفحه‌بندی واقعی در `BaseCrudService` (الان سقف ثابت ۵۰)
- [ ] شهرداری اداری: گردش تأیید و مکاتبات روی پروانه و شکایت

### کنسل‌شده

- **Power BI** — کنسل شد (۲۰۲۶-۰۸-۱۰).  گران، ابری، و با استقرار بدون
  اینترنت ناسازگار.  اگر روزی گزارش‌ساز لازم شد: یک لایهٔ view خواندنی در
  دیتابیس بسازید و Metabase یا Superset را در همان compose بالا بیاورید؛
  آن‌وقت هر ابزار BI دیگری هم وصل می‌شود.

### ۷ — بدهی‌های کوچک
- [ ] `README.md` هنوز Prisma را ذکر می‌کند

---

## نشست ۲۰۲۶-۰۸-۱۱ — فونت فارسی و سه‌زبانه‌سازی

### انجام‌شده ✅

**فونت فارسی — یک باگ استقرار، نه یک مسئلهٔ ظاهری**

`web/app/globals.css` وزیرمتن را از `fonts.googleapis.com` می‌گرفت.  در
استقرار محلی/شبکهٔ داخلی — که همان حالت هدف است — این نشانی در دسترس نیست و
**تمام متن فارسی روی Tahoma می‌افتاد**.

- ۵ وزن (300/400/500/700/800) در `web/public/fonts/` ذخیره شد
- `@import` گوگل با `@font-face` محلی و `font-display: swap` جایگزین شد
- `preload` وزن ۴۰۰ در `app/layout.tsx`
- ⚠️ `web/public/fonts/` نباید از مخزن حذف شود؛ بدون آن دوباره می‌شکند

**سه‌زبانه‌سازی (fa / en / ar) — کامل**

مشکل معماری: `t(key, lang)` نیاز داشت `lang` دستی به هر کامپوننت داده شود،
برای همین فقط ۳ صفحه از ۸ صفحه ترجمه شده بود.

- `web/lib/i18n-context.tsx` (جدید) — `LanguageProvider` + `useI18n()`
  که `{ lang, setLang, t, dir, locale, loading }` می‌دهد
- مقدار اولیه همیشه `fa` است و زبانِ ذخیره‌شده فقط پس از mount خوانده
  می‌شود — وگرنه رندر سرور و کلاینت فرق می‌کنند و React خطای hydration
  می‌دهد.  **این را تغییر ندهید.**
- `document.documentElement.dir/lang` در همین Provider تنظیم می‌شود
- Provider در `app/layout.tsx` سوار شد
- واژه‌نامه: **۲۱۱ کلید** در `web/lib/i18n.ts`
- همهٔ ۸ صفحه + `AppShell` منتقل شدند؛ `dashboard` هم از الگوی قدیمی
  (state محلی) به Context آمد
- `toLocaleString('fa-IR')` و `toLocaleDateString('fa-IR')` با `locale`
  جایگزین شدند — پیش از این در نمای انگلیسی هم ارقام فارسی نشان داده می‌شد

الگو: برچسب در ثابت‌های ماژول (`NAV`، `TABS`، `STAT_CARDS`، `ORDER_STATUS`،
`ITEM_STATUS`، `TABLE_STATUS`، `STATUS`) **کلید ترجمه** است، نه متن؛ متن در
زمان رندر با `t()` ساخته می‌شود.  استثنا: `LANGS` در `AppShell` که
`item.label` نام خود زبان است و **نباید** ترجمه شود.

`npx tsc --noEmit` پاک است.

### باقی‌مانده در همین موضوع

- [ ] `app/layout.tsx` metadata، `app/offline/page.tsx` و
      `app/sw-register.tsx` هنوز فارسی ثابت دارند (کامپوننت سرور /
      خارج از Provider — نیاز به راهکار جدا)
- [ ] رسید حرارتی `web/lib/receipt.ts` ترجمه نشده
- [ ] پیام‌های خطای بک‌اند همه فارسی‌اند

### 🔴 شکاف مهمی که پیدا شد

**رستوران سفارش‌گیری ندارد.**  `web/app/restaurant/page.tsx` می‌تواند میز،
سفارش، آشپزخانه و منو را *نمایش* دهد، ولی هیچ مسیری برای **ثبت سفارش جدید**
ندارد — همان وضعیتی که فروشگاه پیش از ساخت POS داشت.  بدون این، محصول
رستوران قابل بهره‌برداری نیست.  این باید اولویت بعدی رستوران باشد، نه
بازطراحی ظاهری.

### ابزار جانبی (بی‌ربط به پروژه)

`rtl-for-claude` در `D:\aziz\rtl-for-claude` گذاشته شد — فقط نمایش فارسی در
رابط خود Claude را درست می‌کند.  وصلهٔ اپ دسکتاپ **فقط macOS** است و روی
ویندوز کار نمی‌کند؛ روی ویندوز فقط افزونهٔ مرورگر و افزونهٔ VS Code.

### ⚠️ هشدار پابرجا

هنوز **هیچ چیز commit نشده** — بیش از ۲۲۰ فایل تغییریافته بدون git.
این بزرگ‌ترین ریسک عملیاتی پروژه است.

---

## زنجیرهٔ فروش ERP (۲۰۲۶-۰۸-۱۱)

`Quotation` / `SalesOrder` / `Shipment` در دیتابیس بودند ولی سرویس‌هایشان
پوستهٔ CRUD خالی بود.  حالا منطق واقعی دارند.

**`sql/migrations/009_sales_chain.sql`**
- شمارهٔ اسناد از UNIQUE **سراسری** به یکتای **(companyId, no)** — پیش از
  این دو شرکت نمی‌توانستند هر دو «Q-001» داشته باشند (همان باگ `Account.code`)
- ستون‌های پیوند: `SalesOrder.quotationId/saleId/warehouseId`,
  `Shipment.salesOrderId`, `SalesOrderItem.shippedQty`
- FK با CASCADE (تا حالا هیچ‌کدام FK نداشتند)
- CHECK روی وضعیت‌ها + قید `shippedQty <= qty`

**`src/sales-orders/sales-chain.service.ts`** — ۷ اندپوینت زیر `/sales-chain`

تصمیم‌های مهم که نباید عوض شوند:
1. صدور فاکتور به `SalesService.create` واگذار شده، بازنویسی نشده — کسر
   موجودی، سند حسابداری و بهای تمام‌شده باید یک مسیر داشته باشند.  به همین
   دلیل `invoiceOrder` تراکنش خودش را باز نمی‌کند (تراکنش داخل SalesService است).
2. وضعیت سفارش از روی مجموع `shippedQty` محاسبه می‌شود، نه ورودی کلاینت.
3. شرط وضعیت داخل خود `UPDATE` است تا دو درخواست هم‌زمان نتوانند دو سفارش
   از یک پیش‌فاکتور (یا دو فاکتور از یک سفارش) بسازند.

**`web/app/sales-chain/page.tsx`** — سه تب، سه‌زبانه، دکمه‌های ۴۴px.

`tsc --noEmit` در هر دو طرف پاک.  `schema.generated.ts` دستی همگام شد.

### باقی‌مانده در این موضوع
- [ ] فرم ساخت پیش‌فاکتور در وب (الان فقط API دارد)
- [ ] ارسال باید موجودی را رزرو/کسر کند؛ الان کسر موجودی فقط هنگام فاکتور است
- [ ] migration 009 روی دیتابیس واقعی اجرا نشده (Docker بالا نبود)
- [ ] CRM هنوز پوستهٔ خالی است — قدم بعدی: سرنخ ← فرصت ← اتصال به پیش‌فاکتور

### ESB — تصمیم
ESB کامل ساخته **نشد** و پیشنهاد شد ساخته نشود: n8n مسیریابی بیرونی را
پوشش می‌دهد و بروکر پیام یک سرویس سنگین به استقرار لوکال اضافه می‌کند.
جایگزین سبک پیشنهادی: `EventEmitterModule` داخلی NestJS برای رویدادهای
دامنه (`SaleCreated`, `ShipmentDelivered`) — بدون سرویس اضافه.

---

## نشست ۲۰۲۶-۰۸-۱۲ — سیستم بالا آمد + کاردکس + مرجوعی

### ✅ سیستم اجرا شد و تست شد

`.env` وجود نداشت؛ ساخته شد. **`WEB_PORT=3002`** (پورت ۳۰۰۱ روی این
دستگاه اشغال بود).  محصول: `store`.  نشانی: `192.168.100.60`

    وب      http://192.168.100.60:3002
    API     http://192.168.100.60:3000/api
    ورود    admin@molido.ai / admin123

migrationها تا 009 روی دیتابیس واقعی اجرا شدند (`shippedQty` تأیید شد).

### 🔴 باگ مهمی که در تست پیدا شد — رفع شد

**منوی وب از محصول انتخابی بی‌خبر بود.**  با `MOLIDO_PRODUCT=store`
بک‌اند ماژول رستوران را بالا نمی‌آورد (۴۰۴)، ولی «رستوران» در منو دیده
می‌شد و کاربر را به صفحه‌ای می‌برد که همهٔ درخواست‌هایش ۴۰۴ می‌گرفت.
جداسازی محصول در بک‌اند ساخته شده بود ولی سمت وب هرگز وصل نشده بود.

- `web/lib/product.ts` (جدید) — آینهٔ `backend/src/product.ts`
- `NEXT_PUBLIC_MOLIDO_PRODUCT` از `MOLIDO_PRODUCT` در compose مشتق می‌شود
- `NavItem.feature` — آیتم بدون قابلیت در منو نمی‌آید

⚠️ تغییر محصول در `.env` نیاز به `--build` دارد (متغیر NEXT_PUBLIC هنگام
build جاسازی می‌شود).

### ✅ صفحهٔ انبار — `web/app/inventory/page.tsx`

موجودی، فیلتر انبار، جستجو، «فقط رو به اتمام»، اصلاح موجودی، انتقال بین
انبارها.  تست شد: ۴۲ → +۱۰ → ۵۲ → −۵ → ۴۷، و کاهش ۹۹۹۹۹ **رد شد**
(«موجودی نمی‌تواند منفی شود») — محافظ کف صفر سالم است.

### ✅ کاردکس (حرکت موجودی) — `010_stock_movements.sql`

مشکل ریشه‌ای: `Inventory` فقط «مقدار فعلی» را داشت.  سیستم می‌گفت ۴۷ عدد
ولی نمی‌توانست بگوید **چرا ۴۷**.

- `StockMovement` — دلتا، مانده پس از حرکت، دلیل، سند مبدأ، کاربر
- `StockCount` + `StockCountLine` — انبارگردانی، با نمایهٔ جزئی
  «فقط یک شمارش باز برای هر انبار»
- `applyStockDelta` حالا پارامتر `context` می‌گیرد و حرکت را **در همان
  تراکنش** ثبت می‌کند.  چون این تنها نقطهٔ تغییر موجودی در کل سیستم است
  (۶ فراخوان)، هیچ مسیری نمی‌تواند از قلم بیفتد.
- زمینه به فروش، لغو فروش، خرید، اصلاح و انتقال داده شد.
- در `SalesService.create` شناسهٔ فاکتور **پیش از** کسر موجودی ساخته
  می‌شود تا حرکت انبار بتواند به آن ارجاع دهد — این ترتیب را به‌هم نزنید.

### 🔸 نیمه‌کاره — برگشت از فروش/خرید

`011_returns.sql` **نوشته شد ولی سرویسش نوشته نشده**.  محتوای migration:
شمارهٔ مرجوعی در سطح شرکت، ستون `type` (SALE/PURCHASE)، انبار، روش و مبلغ
عودت، `SaleItem.returnedQty` و `PurchaseItem.returnedQty` با قید
`returnedQty <= quantity` (بدون آن می‌شود ۳ عدد خرید و ۱۰ عدد مرجوع کرد)،
و قید «مرجوعی باید سند مبدأ داشته باشد».

قدم بعدی: `ReturnsService` واقعی — برگرداندن موجودی با
`reason: 'RETURN'`، عودت وجه از صندوق یا بستانکار کردن مشتری، و سند
معکوس با `PostingService.reverseBySourceIn`.

### باقی‌مانده
- [ ] migration 010 و 011 روی دیتابیس اجرا نشده‌اند
- [ ] `schema.generated.ts` برای جدول‌های 010/011 همگام نشده
- [ ] صفحهٔ کاردکس و صفحهٔ انبارگردانی در وب
- [ ] `StockCount` سرویس ندارد
- [ ] ۵۰ ماژول دیگر هنوز پوستهٔ خالی‌اند (فهرست: با سرویس زیر ۲۵ خط)

---

## ادامهٔ ۲۰۲۶-۰۸-۱۲ — مرجوعی کامل شد

### ✅ migration 010 و 011 روی دیتابیس اجرا شدند
`StockMovement`, `StockCount`, `StockCountLine`, و `SaleItem.returnedQty`
تأیید شدند.  `schema.generated.ts` همگام شد (۱۴۵ جدول).

### ✅ `ReturnsService` واقعی — جایگزین CRUD خالی

`backend/src/returns/returns.service.ts` (~۴۵۰ خط) + کنترلر با ۵ اندپوینت
زیر `/returns`.  `AccountingModule` سراسری است، پس ماژول تغییر نخواست.

قواعد تازه در `posting-rules.ts`: `salesReturnEntry`, `returnCogsEntry`,
`purchaseReturnEntry`.

تصمیم‌هایی که نباید عوض شوند:
1. **سند مستقل، نه معکوسِ فاکتور.**  مرجوعی معمولاً جزئی است؛ معکوس کردن
   سند اصلی کل فروش را خنثی می‌کند.
2. **عودت نقدی از صندوق کم می‌شود** — وگرنه در پایان شیفت کسری کاذب
   می‌سازد (همان اشتباهی که یک بار در «لغو فاکتور» رخ داده بود).
3. **قیمت واحد خالص** = قیمت منهای سهم تخفیف همان سطر؛ بدون این، بیش از
   دریافتی عودت می‌شود.
4. **مالیات به نسبت** از فاکتور اصلی گرفته می‌شود، نه نرخ ثابت.
5. سقف مرجوعی **هم** در `WHERE` (برای پیام روشن) **هم** با قید دیتابیس
   (تور ایمنی) کنترل می‌شود.  اول فقط قید بود و خطای خام ۵۰۰ می‌داد.

### ✅ تست چرخهٔ کامل روی دیتابیس واقعی

| مرحله | نتیجه |
|---|---|
| فروش ۳ عدد | موجودی ۵۱ → ۴۸ |
| کاردکس | `SALE −۳` ثبت شد |
| مرجوعی ۱ عدد | موجودی ۴۸ → ۴۹ |
| عودت نقدی | صندوق ۹۳۰٬۰۰۰ → ۶۲۰٬۰۰۰ |
| مرجوعی مازاد ۹۹ | ۴۰۰ با پیام «باقی‌مانده: ۲» |
| سند | ۴۱۰۱ بد/۱۱۰۱ بس + ۱۱۰۴ بد/۵۱۰۱ بس |
| **تراز آزمایشی** | **۰٫۰۰** |

### ✅ `web/app/returns/page.tsx`
جستجوی فاکتور با شماره، انتخاب اقلام و مقدار، چهار روش عودت، دلیل مرجوعی،
و تاریخچه.  سه‌زبانه، ۴۴px.  به منو اضافه شد.

### باقی‌مانده
- [ ] برگشت از خرید فقط API دارد، صفحهٔ وب ندارد
- [ ] صفحهٔ کاردکس و انبارگردانی در وب
- [ ] `StockCount` سرویس ندارد (جدولش هست و ظاهراً حرکت COUNT از جایی ثبت شده)
- [ ] Docker Desktop در این نشست ۳ بار افتاد — ناپایدار است

---

## ۲۰۲۶-۰۸-۱۲ (ادامه) — رابط کاربری، انقضا، کاربران، پشتیبان‌گیری

تصمیم کاربر: **شهرداری فعلاً کنار** (پلن جدید بعداً می‌آید)؛ بقیه ساخته شود.

### ✅ صفحه‌های تازهٔ وب  (۱۰ → ۱۴ صفحه)
- `accounting/` — تراز آزمایشی، سود و زیان، ترازنامه، دفتر روزنامه با
  دکمهٔ سند معکوس.  وضعیت تراز همیشه بالای صفحه دیده می‌شود.
- `purchases/` — سفارش خرید + دریافت کالا، تأمین‌کنندگان، هزینه‌ها
- `treasury/` — حساب‌های بانکی، تراکنش‌ها، چک‌ها (وصول/برگشت)
- `users/` — کاربران، سطوح دسترسی، گزارش فعالیت

`components/ui.tsx` (جدید) — `StatCard`, `Tabs`, `DataTable`, `TOUCH`,
`statusColor`.  پیش از این هر صفحه استایل جدول را از نو می‌نوشت.

### ✅ تاریخ انقضا و سری ساخت — `012_batch_expiry.sql`
مشکل: `expiryDate` فقط روی خودِ کالا بود، یعنی **یک تاریخ برای کل موجودی**.
در عمل هر محموله تاریخ خودش را دارد؛ با تاریخ مشترک یا هشدار زودتر از موعد
می‌آید یا اصلاً نمی‌آید.

- `PurchaseItem`: `batchNo`, `expiryDate`, `manufactureDate` + قید
  «انقضا نمی‌تواند پیش از تولید باشد»
- `BatchNumber` (جدولش بود ولی هیچ‌جا نوشته نمی‌شد) حالا هنگام **دریافت
  کالا** پر می‌شود، با `remainingQty` و نمایهٔ یکتای
  (شرکت، کالا، انبار، سری)
- `GET /inventory/expiring?days=N` — بر پایهٔ محموله، نه کالا

تست شد: خرید ۲۰ عدد با انقضای ۲۰۲۶-۰۹-۰۱ → هشدار «۲۰ روز مانده» ✅

### ✅ پشتیبان‌گیری خودکار — `backup/` (کاملاً تازه؛ **قبلاً وجود نداشت**)
سرویس `backup` در compose، `pg_dump | gzip` شبانه (پیش‌فرض ۰۲:۱۷).

- نوشتن در فایل موقت و تغییر نام در پایان — پشتیبانِ نیمه‌نوشته هرگز نام
  نهایی نمی‌گیرد
- `gzip -t` و بررسی حجم پیش از پذیرش
- حذف نسخه‌های قدیمی **فقط پس از** موفقیت نسخهٔ تازه
- یک پشتیبان در لحظهٔ راه‌اندازی تا تنظیمات غلط همان اول معلوم شود
- ⚠️ `dcron` آلپاین خطوط `VAR=value` در crontab را رد می‌کند؛ متغیرها در
  `/etc/backup.env` نوشته و در خود دستور source می‌شوند.  **این را ساده نکنید.**

**تست بازیابی واقعی**: ۱۴۶ جدول / ۳ فاکتور / ۹ سند — یکسان، صفر خطا.

### وضعیت امنیت
- ✅ bcrypt، JWT، `RolesGuard`، ۸ نقش، رمز هرگز در پاسخ برنمی‌گردد
- ✅ کاربر حذف نمی‌شود بلکه غیرفعال می‌شود (رد حسابرسی)
- ✅ پشتیبان‌گیری خودکار
- ❌ **RLS ندارد** — جداسازی شرکت‌ها فقط در کد است، نه دیتابیس
- ❌ دسترسی فقط در سطح نقش است، نه عملیات (permission)
- ❌ رمزنگاری داده در حالت سکون (at-rest) ندارد
- ❌ `n8n.controller.ts` و `liveness.controller.ts` بدون JwtAuthGuard
  (اولی با secret محافظت می‌شود، دومی عمداً باز است)

### باقی‌مانده
- [ ] صفحهٔ محموله/انقضا در وب (API آماده است)
- [ ] چاپ بارکد، کالر آی‌دی، ویزیتور، دارایی ثابت، چندشرکتی
- [ ] گزارش‌های تفصیلی فروش (روزانه/کالا/مشتری)

---

## ۲۰۲۶-۰۸-۱۲ — RLS: بزرگ‌ترین ریسک امنیتی بسته شد ✅

جداسازی شرکت‌ها دیگر فقط با `WHERE "companyId" = $1` در کد نیست؛ در **سطح
دیتابیس** اعمال می‌شود.  یک `WHERE` فراموش‌شده در هر یک از ۸۵ ماژول دیگر
نشت داده نمی‌سازد.

### معماری — این را به‌هم نزنید
1. `database/tenant-context.ts` — `AsyncLocalStorage` نگهدار `companyId`.
   چرا ALS و نه پارامتر: افزودن پارامتر به صدها متد یعنی صدها نقطهٔ
   فراموش‌شدنی که همان حفره را باز می‌کنند.
2. `database/tenant.interceptor.ts` — زمینه را از `req.user` می‌گیرد.
   **Interceptor** است نه Middleware، چون Guardها (که توکن را باز می‌کنند)
   پس از Middleware و پیش از Interceptor اجرا می‌شوند.
   Observable دستی ساخته می‌شود چون اشتراک باید *داخل* دامنهٔ ALS باشد.
3. `DatabaseService` پیش از هر پرس‌وجو `set_config('app.company_id', …)`
   می‌زند.  اتصال‌ها بازیافت می‌شوند، پس مقدار هر بار **دوباره** نوشته
   می‌شود؛ هرگز به مقدار به‌جامانده تکیه نمی‌شود.
4. `013_row_level_security.sql` — ۹۸ سیاست روی جدول‌های دارای `companyId`.

### چرا دو نقش دیتابیس
- برنامه با **`molido_app`** وصل می‌شود (صاحب جدول‌ها **نیست**) ⇒ RLS بر او
  اعمال می‌شود.
- مهاجرت، داده اولیه و پشتیبان‌گیری با **`postgres`** (صاحب) اجرا می‌شوند و
  طبق رفتار پیش‌فرض PostgreSQL از RLS معاف‌اند.
- اگر برنامه با نقش صاحب وصل شود، **RLS کاملاً بی‌اثر می‌شود**.
- `connection.ts` تابع `adminDatabaseConfig()` دارد که `seed` و `migrate`
  از آن استفاده می‌کنند (`PGADMIN_USER` / `PGADMIN_PASSWORD`).

### fail-closed
زمینهٔ تهی ⇒ **هیچ سطری**.  اگر جایی زمینه را نگذارد، پرس‌وجو خالی برمی‌گردد
و نقص فوراً دیده می‌شود — به‌جای آنکه بی‌سروصدا همه‌چیز را برگرداند.
استثنا: `User` و `Company` هنگام ورود به سامانه (پیش از داشتن زمینه).

### تست
| آزمون | نتیجه |
|---|---|
| بدون زمینه | ۰ سطر ✅ |
| زمینهٔ درست | ۳ فاکتور ✅ |
| زمینهٔ شرکت دیگر | ۰ سطر ✅ |
| درج در شرکت دیگر | `violates row-level security policy` ✅ |
| ورود به سامانه | سالم ✅ |
| فروش کامل (تراکنش+موجودی+کاردکس+سند) | سالم، تراز ۰٫۰۰ ✅ |
| ۳۱ اندپوینت خواندنی | ۳۱/۳۱ ✅ |

### باگ پیدا‌شده در حین تست
`ProductReturn` روی `Customer.name` join می‌کرد؛ آن جدول `firstName`/
`lastName` دارد.  ۵۰۰ می‌داد — رفع شد.

### ⚠️ رمز پیش‌فرض
`molido_app` رمز ثابت `molido_app_local` دارد.  برای استقرار عمومی با
`APP_DB_USER`/`APP_DB_PASSWORD` در `.env` عوضش کنید.  پورت دیتابیس روی LAN
باز نیست، پس در استقرار لوکال ریسک بالایی ندارد.

---

## ۲۰۲۶-۰۸-۱۲ — گزارش‌های تفصیلی فروش ✅

`GET /reports/sales/breakdown?from&to` — هفت برش در یک رفت‌وبرگشت:
مشتری، ساعت، صندوق‌دار، روش پرداخت، سود به تفکیک کالا، مالیات، مرجوعی.

تصمیم‌ها:
- **چند پرس‌وجوی جدا، نه یک GROUP BY بزرگ** — ترکیبشان سطرها را ضرب می‌کند
  و مبالغ را چند بار می‌شمارد؛ همان خطای رایج گزارش‌های دست‌ساز.
- **مبلغ فروش از `Sale.total`** گرفته می‌شود نه جمع سطرها، تا با دفتر کل
  یکی بماند (تخفیف و مالیات عدد دیگری می‌دهند).
- **روش پرداخت از `Payment`** نه `Sale`: یک فاکتور می‌تواند نقد + کارت باشد.
- **فروش خالص = فروش − مرجوعی**؛ گزارشی که مرجوعی را کم نکند فروش را بیش
  از واقع نشان می‌دهد.
- حاشیهٔ منفی (فروش زیر قیمت خرید) در صفحه قرمز می‌شود.

⚠️ باگی که در تست پیدا شد: وقتی بازهٔ تاریخ داده نمی‌شد، `$2`/`$3` در SQL
نمی‌آمدند ولی همچنان فرستاده می‌شدند → «bind message supplies 3 parameters».
شمارهٔ پارامتر حالا پویاست (`fromIndex`/`toIndex`).  **اگر شرط تازه‌ای
اضافه کردید، این شماره‌ها را هم به‌روز کنید.**

`web/app/reports/page.tsx` — ۵ تب، ۸ کارت آمار، نمودار میله‌ای بدون
کتابخانه.  به منو اضافه شد.

### وضعیت فعلی
- **۱۴ صفحهٔ وب** (از ۱۰) — همه ۲۰۰ می‌دهند
- ۳۱/۳۱ اندپوینت خواندنی سالم زیر RLS
- تراز آزمایشی ۰٫۰۰

---

## ۲۰۲۶-۰۸-۱۲ — چاپ بارکد + دارایی ثابت و استهلاک ✅

### چاپ برچسب بارکد
`web/lib/barcode.ts` — EAN-13 و CODE-128 به SVG، **بدون هیچ کتابخانه**.
کتابخانه‌ها یا canvas می‌خواهند (کیفیت چاپ افت می‌کند) یا صدها کیلوبایت به
باندل اضافه می‌کنند؛ خودِ الگو چند ده خط است و SVG در هر DPI تیز است.

`web/app/labels/page.tsx` — سه اندازهٔ برچسب، تعداد دلخواه، نمایش نام/قیمت،
پیش‌نمایش در اندازهٔ واقعی، چاپ در پنجرهٔ جدا با `@page` دقیق.

⚠️ چاپ در پنجرهٔ جداست نه `@media print` روی همان صفحه: برچسب باید سیاه روی
سفید و بدون تم تیره چاپ شود و اندازهٔ صفحه دقیقاً اندازهٔ برچسب باشد.

**تست: ۱۰/۱۰** — رقم کنترل با مقادیر مرجع EAN-13 (`5901234123457`,
`4006381333931`) و عرض دقیقاً ۹۵ واحد.

### دارایی ثابت و استهلاک — `014_fixed_assets.sql`
`Asset` همهٔ ورودی‌ها را داشت (بهای خرید، ارزش اسقاط، عمر مفید) ولی هیچ‌گاه
استهلاکی محاسبه نمی‌شد: دارایی‌ها تا ابد به ارزش خرید می‌ماندند و سود هر
دوره بیش از واقع گزارش می‌شد.

- `AssetDepreciation` — یک سطر برای هر دارایی در هر ماه، با **نمایهٔ یکتا**
- خط مستقیم و نزولی؛ آخرین ماه دقیقاً باقی‌مانده را می‌برد
- قید دیتابیس: استهلاک انباشته ≤ (بهای خرید − ارزش اسقاط)
- **یک سند برای کل دوره**، نه یک سند به‌ازای هر دارایی

⚠️ **تداخل کد حساب که در تست پیدا شد**: `5201` در سرفصل‌های موجود «حقوق و
دستمزد» بود؛ هزینهٔ استهلاک روی حساب حقوق می‌نشست.  به **`5205`** منتقل شد.

| آزمون | نتیجه |
|---|---|
| ۱۲٬۰۰۰٬۰۰۰ ÷ ۵ سال | ماهانه **۲۰۰٬۰۰۰** ✅ |
| اجرای دوبارهٔ همان ماه | **۰** ✅ |
| ماه بعد | ۲۰۰٬۰۰۰ ✅ |
| سند: ۵۲۰۵ بد / ۱۲۰۲ بس | ✅ |
| واگذاری ۱۲٬۰۰۰٬۰۰۰ (دفتری ۱۱٬۶۰۰٬۰۰۰) | سود **۴۰۰٬۰۰۰** ✅ |
| سند واگذاری ۴ قلمی | ✅ |
| **تراز آزمایشی** | **۰٫۰۰** ✅ |

باگ رفع‌شده: `$1` هم مقدار ستون بود هم داخل `CASE` — «inconsistent types
deduced». cast صریح `::numeric` اضافه شد.

### باقی‌مانده
- [ ] صفحهٔ وب دارایی ثابت (API کامل است)
- [ ] بستن سال مالی
- [ ] کارت‌خوان (PSP)، ویزیتور، کالر آی‌دی، چندشرکتی

---

## ۲۰۲۶-۰۸-۱۲ — صفحهٔ دارایی + بستن سال مالی ✅

### `web/app/assets/page.tsx`
چهار کارت آمار، فرم ثبت دارایی، اجرای استهلاک با انتخاب دوره، واگذاری.

### بستن سال مالی — دو ایراد جدی رفع شد

**۱. سند اختتامیه صادر نمی‌شد.**  `closeFiscalYear` فقط وضعیت را به
`CLOSED` تغییر می‌داد.  یعنی حساب‌های موقت (درآمد/هزینه) هرگز صفر نمی‌شدند و
صورت سود و زیانِ سال بعد سود همهٔ سال‌های قبل را هم نشان می‌داد؛ سود انباشته
(۳۱۰۲) هم هیچ‌وقت پر نمی‌شد.

حالا: هر حساب درآمد بدهکار و هر حساب هزینه بستانکار می‌شود و تفاوت به
«سود و زیان انباشته» منتقل می‌شود — همه در **یک تراکنش** با خودِ بستن سال.

مبنای محاسبه **اقلام سند** است نه ستون `balance`، چون `balance` مانده
تجمعی همهٔ سال‌هاست.

**۲. سند در سال بسته پذیرفته می‌شد.**  `PostingService.postIn` وضعیت سال را
اصلاً نگاه نمی‌کرد.  یعنی یک فروش با تاریخ گذشته پس از بستن سال،
صورت‌های مالیِ نهایی‌شده را بی‌سروصدا عوض می‌کرد.  محافظ اضافه شد
(`allowClosedYear` فقط برای خود سند اختتامیه).

**محافظ سوم:** اگر سند پیش‌نویس باقی مانده باشد، بستن سال رد می‌شود —
وگرنه آن سند پس از بستن سال هرگز قابل تأیید نبود.

| آزمون | نتیجه |
|---|---|
| سود پیش از بستن | ۷۲۰٬۰۰۰ |
| `netIncome` سند اختتامیه | **۷۲۰٬۰۰۰** ✅ |
| سود انباشته (۳۱۰۲) پس از بستن | **۷۲۰٬۰۰۰** ✅ |
| حساب‌های موقت (۴ حساب) | همه **۰٫۰۰** ✅ |
| فروش در سال بسته | رد شد ✅ |
| بستن دوباره | رد شد ✅ |
| **تراز آزمایشی** | **۰٫۰۰** ✅ |

⚠️ `LedgerService` حالا `PostingService` می‌گیرد — سازندهٔ دو‌آرگومانی.

### باقی‌مانده
- [ ] صفحهٔ وب بستن سال مالی (API آماده)
- [ ] برگشت از خرید صفحهٔ وب ندارد
- [ ] کاردکس و انبارگردانی در وب
- [ ] ویزیتور، کارت‌خوان PSP، کالر آی‌دی، چندشرکتی

---

## ۲۰۲۶-۰۸-۱۲ — انبارگردانی/کاردکس + ویزیتور و کمیسیون ✅

### `web/app/stock-count/page.tsx`
دو تب: شمارش‌ها و کاردکس.  ورود مقدار شمرده‌شده با `onBlur` (نه `onChange`
— هر ضربهٔ کلید یک PATCH می‌فرستاد)، ستون اختلاف با رنگ‌بندی، اعمال و لغو.
کاردکس با محافظ `cancelled` تا پاسخ کهنه روی تازه ننشیند.

بهبود API: `stock-count POST` حالا اقلام را همراه سربرگ برمی‌گرداند.

**تست چرخه:** موجودی ۶۷ → شمارش ۶۰ → کسری ۷ شناسایی → موجودی ۶۰ →
کاردکس `COUNT −۷` ✅ ؛ شمارش موازی روی یک انبار رد شد ✅

### ویزیتور و کمیسیون — `015_sales_agents.sql`
`SalesAgent` نرخ کمیسیون و هدف داشت ولی **هیچ فاکتوری به ویزیتور وصل
نمی‌شد** — نرخ ثبت می‌شد و هرگز روی چیزی اعمال نمی‌گشت.

- `Sale.salesAgentId` و `Customer.salesAgentId`
- `AgentCommission` — نرخ **در لحظهٔ محاسبه تثبیت** می‌شود تا تغییر بعدی
  نرخ، کمیسیون‌های گذشته را عوض نکند
- کمیسیون روی **فروش خالص** (منهای مرجوعی)؛ وگرنه می‌شد فروخت، کمیسیون
  گرفت و بعد مرجوع کرد
- حساب‌های ۵۲۰۶ (هزینهٔ کمیسیون) و ۲۱۰۶ (کمیسیون پرداختنی)
- در `SalesService.create`: اگر ویزیتور صریح داده نشود، از ویزیتور ثابتِ
  مشتری برداشته می‌شود

| آزمون | نتیجه |
|---|---|
| نرخ ۱۵۰٪ | رد شد ✅ |
| فروش ۱۰٬۰۰۰٬۰۰۰ با ویزیتور ۵٪ | کمیسیون **۵۰۰٬۰۰۰** ✅ |
| سند ۵۲۰۶ بد / ۲۱۰۶ بس | ✅ |
| محاسبهٔ دوباره (۳ بار) | همان ۵۰۰٬۰۰۰، بدون تکرار ✅ |
| تراز | **۰٫۰۰** ✅ |

باگ رفع‌شده: محاسبهٔ دوباره سند تکراری می‌زد و
`JournalEntry_source_key` را می‌شکست.  حالا سند قبلی **معکوس** می‌شود و
سند تازه صادر می‌گردد — همان الگوی لغو فاکتور و مرجوعی.

### 🔴 `016_rls_sync.sql` — نقص فرآیندی که پیدا شد
جدول‌های تازه (`AssetDepreciation`, `AgentCommission`) **بدون سیاست RLS**
ساخته شده بودند و بی‌سروصدا داده‌های همهٔ شرکت‌ها را برمی‌گرداندند.

⚠️ **قاعده از این پس:** هر مهاجرتی که جدولی با ستون `companyId` می‌سازد،
باید بلوک `016_rls_sync.sql` را هم اجرا کند.  این فایل عمداً
«اجرای دوباره بی‌خطر» است.

### باقی‌مانده
- [ ] صفحهٔ وب ویزیتور و کمیسیون
- [ ] صفحهٔ بستن سال مالی
- [ ] برگشت از خرید در وب
- [ ] کارت‌خوان PSP، کالر آی‌دی، چندشرکتی

---

## ۲۰۲۶-۰۸-۱۲ — سه صفحهٔ پایانی + آزمون یکپارچه ✅

### صفحه‌های تازه
- `web/app/sales-agents/page.tsx` — ویزیتورها با نوار پیشرفت هدف، و
  کمیسیون‌ها با پرداخت
- `web/app/fiscal-year/page.tsx` — تعریف سال مالی و بستن با نمایش صریح
  سود، شماره سند و تعداد حساب‌های بسته‌شده
- `web/app/returns/page.tsx` — **برگشت از خرید** به صفحهٔ مرجوعی اضافه شد
  (دو حالت: فروش / خرید)

### 🧪 `backend/test/integration.sh` — آزمون یکپارچه، قابل تکرار

    bash backend/test/integration.sh

**۶۵ آزمون، ۶۵ موفق، ۰ ناموفق:**

| بخش | آزمون |
|---|---|
| اندپوینت‌های خواندنی | ۴۰ مسیر |
| موجودی | کاهش زیر صفر رد شد |
| فروش | ثبت + مبلغ درست |
| کاردکس | حرکت `SALE` ثبت شد |
| مرجوعی | ثبت شد؛ مرجوعی مازاد رد شد |
| استهلاک | تکرار همان ماه = ۰ |
| کمیسیون | تکرار = همان مبلغ |
| RLS | بدون زمینه ۰ سطر؛ **۰ جدول بی‌محافظ** |
| دفتر کل | تراز **۰٫۰۰** |
| وب | ۱۵ صفحه ۲۰۰ |

### آمار پروژه
| | |
|---|---|
| صفحهٔ وب | **۲۰** |
| migration | **۱۶** |
| کلید ترجمه (fa/en/ar) | **۶۳۵** |
| جدول | **۱۴۷** |
| ماژول با منطق واقعی | **۳۸** |
| ماژول پوستهٔ خالی | ۴۷ (بیشترشان شهرداری) |

### وضعیت: هستهٔ فروشگاه کامل و قابل بهره‌برداری
دفتر کل دوطرفه با بستن سال، انبار با کاردکس و انبارگردانی، فروش و صندوق و
شیفت، خرید، مرجوعی دوطرفه، زنجیرهٔ پیش‌فاکتور تا فاکتور، دارایی ثابت و
استهلاک، ویزیتور و کمیسیون، کالابرگ، چاپ بارکد، RLS کامل، سه‌زبانه.

### باقی‌مانده (نیاز به تصمیم شما)
- [ ] **شهرداری** — منتظر برنامهٔ جدید شما
- [ ] CRM واقعی (سرنخ ← فرصت) — هنوز پوسته
- [ ] منابع انسانی: حضور و غیاب، مرخصی، ارزیابی
- [ ] کارت‌خوان PSP، کالر آی‌دی، چندشرکتی
- [ ] ⚠️ **هنوز هیچ چیز commit نشده**

---

## ۲۰۲۶-۰۸-۱۳ — CRM واقعی + رفع باگ چرخش رمز ✅

### باگ رفع‌شده: چرخش رمز نقش دیتابیس
مهاجرت ۰۱۳ نقش `molido_app` را با رمز پیش‌فرض می‌ساخت (فایل SQL به متغیر
محیطی دسترسی ندارد).  اگر کاربر `APP_DB_PASSWORD` را در `.env` عوض می‌کرد،
بک‌اند با رمز تازه وصل می‌شد ولی رمز نقش قدیمی می‌ماند — اتصال با خطای
احراز هویت می‌شکست بی‌آنکه علتش روشن باشد.

`migrate.ts` حالا پس از مهاجرت‌ها `syncAppRolePassword()` را اجرا می‌کند.
دستور با `format('ALTER ROLE %I WITH PASSWORD %L')` در سمت سرور ساخته
می‌شود، نه با الحاق رشته.  تست شد: رمز عوض شد، بک‌اند بالا آمد، ورود سالم.

### CRM — `017_crm.sql`
ماژول `crm` یک CRUD روی `LoyaltyAccount` بود؛ «باشگاه مشتریان» را CRM
نامیده بودند و هیچ قیف فروشی وجود نداشت.

سه جدول: `Lead`، `Opportunity`، `Interaction` — هر سه با RLS.

**تصمیم اصلی: CRM جزیره‌ای ساخته نشد.**  سرنخ به مشتری تبدیل می‌شود و
تعامل‌هایش هم منتقل می‌شوند؛ فرصت به زنجیرهٔ فروش موجود وصل است.

قیدهای دیتابیس که رفتار را تضمین می‌کنند:
- `Opportunity_lost_chk` — باخت بدون دلیل پذیرفته نمی‌شود.  تنها ارزش
  واقعی ثبت باخت، گزارش «چرا می‌بازیم» است.
- `Interaction_link_chk` — تعامل باید به سرنخ، فرصت یا مشتری وصل باشد؛
  تعاملِ معلق در هیچ گزارشی دیده نمی‌شود.
- نمایهٔ جزئی روی پیگیری‌های سررسیدشده — پرکاربردترین پرس‌وجوی روزانه.

احتمال با مرحله همگام می‌شود (برد ۱۰۰٪، باخت ۰٪) وگرنه پیش‌بینی قیف خراب
می‌ماند.  فرصت بسته دیگر تغییر نمی‌کند.

`web/app/crm/page.tsx` — چهار تب: سرنخ، فرصت، تعامل، و قیف نواری.

### آزمون
`backend/test/crm.sh` — **۱۷/۱۷**.  `integration.sh` — **۶۵/۶۵** (دو بار
پشت سر هم برای اطمینان).

⚠️ **تلهٔ محیطی که وقت گرفت**: پوستهٔ ویندوز متن فارسی را در `curl -d` به
`?` تبدیل می‌کند و شکست کاذب می‌سازد.  خودِ برنامه یونیکد را درست ذخیره
می‌کند (با بایت‌های hex تأیید شد).  یادداشتش بالای هر دو فایل تست هست.

### وضعیت
- ۲۱ صفحهٔ وب، ۱۷ migration، ۱۵۰ جدول، ۱۰۳ سیاست RLS
- تراز آزمایشی صفر، همهٔ محافظ‌ها فعال

---

## ۲۰۲۶-۰۸-۱۳ — منابع انسانی + کرایه حمل ✅

### منابع انسانی — `018_hr.sql`
سه ماژول پوستهٔ خالی بودند (۱۳ خط CRUD): حضور ثبت می‌شد ولی اضافه‌کاری
محاسبه نمی‌شد، مرخصی تأیید می‌شد ولی از مانده کم نمی‌کرد، و **فیش حقوق
هیچ سند حسابداری نمی‌زد** — هزینهٔ حقوق در صورت سود و زیان اصلاً نمی‌آمد.

- ساعت کارکرد **در سرور** محاسبه می‌شود (۸ ساعت عادی، مازاد اضافه‌کاری).
  اگر کلاینت عدد بفرستد، همان عددی که حقوق را می‌سازد قابل دستکاری است.
- نمایهٔ یکتای (کارمند، تاریخ) — ثبت دوباره به‌روز می‌کند، نه دو برابر.
- `LeaveBalance` با قید `used <= entitled + carriedOver` در دیتابیس.
- تأیید مرخصی، کسر از مانده و علامت‌زدن روزها در حضور، **یک تراکنش**.
- سند حقوق در لحظهٔ **تأیید** زده می‌شود نه پرداخت: تعهد از همان لحظه
  وجود دارد و باید در ترازنامه دیده شود.

⚠️ **`hr` به `store` و `resto` اضافه شد.**  قبلاً فقط در نسخهٔ کامل بود،
ولی هر فروشگاهی صندوق‌دار دارد — شیفت و حقوق کار روزمره است نه امکانات
سازمانی.  `web/lib/product.ts` هم همگام شد.

### کرایه حمل — `019_freight.sql`
دو سمت دارد و حسابداری‌شان یکی نیست:

**ورودی (خرید)** — بخشی از بهای تمام‌شدهٔ کالاست، نه هزینهٔ دوره.  به نسبت
**ارزش** روی اقلام سرشکن می‌شود و در `purchasePrice` می‌نشیند.  اگر هزینه
شود، بهای موجودی کمتر از واقع می‌ماند و سود ناخالص بیش از واقع گزارش
می‌شود — و در ماه فروش ناگهان افت می‌کند.

`allocateFreight()` تابع خالص است و آخرین قلم باقی‌مانده را می‌گیرد تا
مجموع سهم‌ها دقیقاً برابر کرایه بماند.  **۸ آزمون** برای همین ریاضی.

**خروجی (فروش)** — درآمد حمل (۴۱۰۶) جدا از هزینه (۵۲۰۴) ثبت می‌شود؛
قاطی کردنشان یعنی هیچ‌وقت معلوم نمی‌شود توزیع سودده است یا زیان‌ده.

`capitalizeFreight` صریح است، نه ضمنی — اثرش روی سود ناخالص مستقیم است.

### آزمون‌ها
| فایل | نتیجه |
|---|---|
| `integration.sh` | ۶۵/۶۵ |
| `hr.sh` | ۲۰/۲۰ |
| `crm.sh` | ۱۷/۱۷ |
| `freight.sh` | ۱۰/۱۰ |
| **جمع** | **۱۱۲** |

`hr.sh` اول تکرارپذیر نبود (شمارهٔ پرسنلی تکراری ⇒ ۱۹ شکست زنجیره‌ای که
هیچ‌کدام باگ واقعی نبودند).  حالا خودش را تمیز می‌کند.

### وضعیت
۱۵۲ جدول، ۱۰۴ سیاست RLS، **صفر جدول بی‌محافظ**، تراز ۰٫۰۰

---

## ۲۰۲۶-۰۸-۱۳ — فروشگاه اینترنتی + آیکون SVG ✅

### آیکون SVG به‌جای emoji
`web/components/icons.tsx` — ۳۵ آیکون درون‌خطی (Lucide)، بدون وابستگی.
۴۲ مورد emoji در ناوبری، کارت آمار و دکمه‌ها جایگزین شد.

emoji سه ایراد داشت: در هر سیستم‌عامل شکل متفاوتی دارد، رنگش با تم و
وضعیت هماهنگ نمی‌شود، و صفحه‌خوان آن را با نام کامل و بی‌ربط می‌خواند.

⚠️ طراحیِ پیشنهادی ابزار UI (Exaggerated Minimalism، فونت لاتین، تم روشن)
کنار گذاشته شد — برای صفحهٔ فرود بازاریابی است نه پنل عملیاتی.  فقط
چک‌لیست تحویل برداشته شد.

### فروشگاه اینترنتی — `020_shop.sql`
محصول تازه: کاربرش **مشتری** است نه کارمند.

**مانع معماری که حل شد:** RLS در حالت fail-closed است و مشتری توکن ندارد،
پس صفحهٔ عمومی هیچ کالایی نمی‌دید.  `ShopTenantMiddleware` زمینهٔ شرکت را
**از پیکربندی سرور** می‌گذارد (`SHOP_COMPANY_ID`، یا تنها شرکت موجود).
عمداً از هدر یا پارامتر خوانده نمی‌شود — وگرنه هر کسی می‌توانست داده‌های
هر شرکتی را ببیند، یعنی همان حفره‌ای که RLS برای بستنش ساخته شد.

**تصمیم اصلی: زنجیرهٔ فروش بازنویسی نشد.**  سفارش آنلاین پس از تأیید به
همان `SalesOrder` تبدیل می‌شود و از آنجا ارسال، فاکتور و سند حسابداری مثل
هر سفارش دیگری جلو می‌رود.

سه محافظ:
1. **قیمت در تسویه دوباره از دیتابیس خوانده می‌شود** — قیمتِ سبد فقط برای
   نمایش است؛ مبنا قرار دادنش یعنی هر کسی کالا را به قیمت دلخواه می‌خرد.
2. **موجودی در لحظهٔ ثبت سفارش** بررسی می‌شود، نه افزودن به سبد.
3. پیام ورودِ ناموفق برای «شماره نیست» و «رمز غلط» یکسان است.

مشتری حضوریِ موجود با ثبت‌نام آنلاین **رکورد تازه نمی‌گیرد** — تاریخچهٔ
خریدش یکی می‌ماند.

⚠️ `x-customer-id` شناسهٔ ساده است نه توکن امضاشده.  برای انتشار روی
اینترنت باید به JWT مستقل مشتری ارتقا یابد.

### باگ واقعی که این کار آشکار کرد
نمایهٔ یکتای `(companyId, phone)` تبدیل سرنخ را می‌شکست وقتی آن شماره از
قبل مشتری بود.  رفتار درست پیوند به همان مشتری است نه ساخت تکراری —
`convertLead` اصلاح شد.  تاریخچهٔ خرید دیگر دو تکه نمی‌شود.

### آزمون‌ها
| فایل | نتیجه |
|---|---|
| `integration.sh` | ۶۵ |
| `shop.sh` | ۲۴ |
| `hr.sh` | ۲۰ |
| `crm.sh` | ۱۷ |
| `freight.sh` | ۱۰ |
| **جمع** | **۱۳۶** |

۱۵۸ جدول، صفر جدول بی‌محافظ.

⚠️ آزمون‌ها روی یک دیتابیس مشترک اجرا می‌شوند و گاهی به‌خاطر رقابت،
یک مورد تک‌موردی می‌شکند.  هر بار جای دیگری است و اجرای دوباره سبز
می‌شود — باگ نیست.  برای CI باید هر مجموعه دیتابیس خودش را بگیرد.

---

## ۲۰۲۶-۰۸-۱۳ — صفحه‌های وب فروشگاه ✅

شش صفحه زیر `web/app/shop/`: کاتالوگ، کالا، سبد و تسویه، ورود/ثبت‌نام،
فهرست سفارش‌ها، پیگیری سفارش.

### چرا قالب جدا از پنل
پنل تم تیره دارد چون کارمند ساعت‌ها با آن کار می‌کند و چگالی داده بالاست.
فروشگاه روشن است چون مشتری چند دقیقه می‌ماند و عکس کالا روی زمینهٔ روشن
بهتر دیده می‌شود.

متغیرهای CSS با پیشوند `--s` و در دامنهٔ `.shop` تعریف شده‌اند تا با
`globals.css` تداخل نکنند.  `AppShell` عمداً استفاده نشد: ناوبری کارکنان
و بررسی توکن دارد که مشتری نباید ببیند.

`lib/shop-api.ts` جدا از `lib/api.ts` است — وگرنه باز بودن پنل در یک تب
باعث می‌شد درخواست‌های فروشگاه توکن کارمند ببرند.

### ایراد واقعی که پیدا و رفع شد
کاتالوگ اول کامپوننت کلاینت بود، پس **HTML اولیه خالی** می‌آمد.  برای پنل
اهمیتی ندارد ولی فروشگاه باید در نتایج جستجو دیده شود و روی اتصال کند هم
چیزی نشان دهد.

کاتالوگ و صفحهٔ کالا به **کامپوننت سرور** تبدیل شدند؛ فقط دکمهٔ افزودن به
سبد کلاینت ماند.  نتیجه: HTML از ۹٫۴KB به ۱۸KB رسید و کالاها با قیمت در
آن هستند.  صفحهٔ کالا `generateMetadata` دارد.

`lib/shop-server.ts` از `INTERNAL_API_URL` استفاده می‌کند
(`http://backend:3000`): سرور Next داخل شبکهٔ داکر است و
`NEXT_PUBLIC_API_URL` نشانیِ مرورگر است، نه قابل استفاده از داخل کانتینر.

### وضعیت
۲۷ صفحهٔ وب (۲۱ پنل + ۶ فروشگاه)، ۲۰ migration، ۱۵۸ جدول.
آزمون: shop ۲۴، integration ۶۵، hr ۲۰، crm ۱۷، freight ۱۰ — **۱۳۶**.

### باقی‌مانده
- [ ] درگاه پرداخت (فعلاً فقط پرداخت در محل)
- [ ] `x-customer-id` باید پیش از انتشار روی اینترنت به JWT تبدیل شود
- [ ] سبد مهمان پس از ورود به حساب منتقل نمی‌شود
- [ ] تصویر کالا: ستون `imageUrl` هست ولی صفحهٔ آپلود ندارد
- [ ] اپ موبایل و اپ دسکتاپ

---

## ۲۰۲۶-۰۸-۱۳ — اپ موبایل، اپ دسکتاپ، ثبت فاکتور خرید ✅

### اپ موبایل — اسکن بارکد با دوربین
`web/components/BarcodeScanner.tsx` — با `BarcodeDetector` بومی مرورگر،
**بدون کتابخانه**.  ZXing و QuaggaJS چند صد کیلوبایت وزن دارند و روی
گوشی ارزان کند می‌شوند؛ جایی که قابلیت بومی نباشد، ورود دستی همیشه هست.

سه نکتهٔ عملی: دوربین **پشت** درخواست می‌شود، **کد تکراری تا ۲ ثانیه
نادیده گرفته می‌شود** (دوربین ده‌ها بار در ثانیه همان بارکد را می‌بیند و
بدون این محافظ یک اسکن ده قلم اضافه می‌کرد)، و **جریان دوربین در هر خروج
بسته می‌شود** وگرنه باتری تمام می‌شود.

وصل شد به: صندوق، انبار، و فاکتور خرید.
manifest: چهار میان‌بر و `portrait-primary`.

### اپ دسکتاپ — عامل چاپ به‌جای Electron
نصب PWA همین حالا پنجرهٔ اپ دسکتاپ می‌دهد؛ آنچه کم بود **دسترسی به
سخت‌افزار** بود، نه پنجرهٔ دیگر.

`print-agent/agent.js` — ~۲۰۰ خط، فقط Node، بدون وابستگی.  چاپ بی‌صدا
روی چاپگر حرارتی (ESC/POS) و باز کردن کشوی پول.

| | عامل | Electron |
|---|---|---|
| حجم | ~۲۰KB | ~۱۵۰MB |
| به‌روزرسانی | وب خودکار | توزیع جدا |
| کدپایه | همان وب | نسخهٔ دوم که عقب می‌افتد |

امنیت: فقط روی `127.0.0.1`، فهرست مبدأ مجاز، سقف ۱MB بدنه.
تست شد: status ✅، مبدأ غیرمجاز رد ✅، بدنهٔ خالی ۴۰۰ ✅.

`web/lib/print-agent.ts` — اگر عامل نباشد **خودکار به چاپ مرورگر
برمی‌گردد**؛ نبودنش هیچ‌چیز را نمی‌شکند.  `AbortSignal.timeout` لازم بود
چون اتصال به پورت بسته روی برخی سیستم‌ها ده‌ها ثانیه معلق می‌ماند.

### ثبت فاکتور خرید — `web/app/purchases/new/page.tsx`
صفحهٔ خرید فقط نمایش بود و **فرم ثبت نداشت**.

سه تصمیم از کار واقعی انبار: **اسکن ⇒ افزودن فوری** (فرم جدا برای هر قلم
یعنی کار انجام نمی‌شود)، **اسکن دوباره = افزایش مقدار** (ده کارتن یعنی ده
اسکن)، و **بهای خرید از آخرین خرید پر می‌شود** ولی قابل تغییر است.

کرایه حمل در همین فرم است با گزینهٔ صریح سرشکن.
دو دکمه: «ثبت» و «ثبت و دریافت» — دومی موجودی و سند می‌زند.

### وضعیت
۲۸ صفحهٔ وب، ۲۰ migration، ۱۵۸ جدول، ۱۳۶ آزمون سبز.

### باقی‌مانده
- [ ] درگاه پرداخت
- [ ] `x-customer-id` ⇒ JWT پیش از انتشار روی اینترنت
- [ ] سبد مهمان پس از ورود منتقل نمی‌شود
- [ ] آپلود تصویر کالا
